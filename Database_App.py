import tkinter as tk

window = tk.Tk()

frame = tk.Frame(master=window, width=200, height=200)
frame.pack()

label1 = tk.Label(master=frame, text="Monster Class")
label1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Flying Wyvern", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Fanged Wyvern", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Brute Wyvern", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Bird Wyvern", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Piscine Wyvern", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Levianth", bg="yellow")
button1.place(x=00, y=0)

button1 = tk.Label(master=frame, text="Fanged Beast", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Amphian", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Carapaceon", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Elder Dragon", bg="yellow")
button1.place(x=0, y=0)

button1 = tk.Label(master=frame, text="Other", bg="yellow")
button1.place(x=0, y=0)

window.mainloop()
